apiVersion: v1
kind: Service
metadata:
  name: rag-server
  labels:
    {{- include "rag-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-server.selectorLabels" . | nindent 4 }}



apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-confluence.fullname" . }}
  labels:
    {{- include "agent-confluence.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-confluence.selectorLabels" . | nindent 4 }}

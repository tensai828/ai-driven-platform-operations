apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kb-rag-redis.fullname" . }}-test-connection"
  labels:
    {{- include "kb-rag-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kb-rag-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/session/userinfo operations"""

import logging
from typing import Dict, Any
from agent_argocd.protocol_bindings.mcp_server.mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def session_service__get_user_info() -> Dict[str, Any]:
    '''
    Get the current user's information.

    This function makes an asynchronous GET request to the '/api/v1/session/userinfo' endpoint to retrieve the current user's information. It constructs the request with necessary parameters and data, and handles the response to return the user's info in JSON format.

    Args:
        None

    Returns:
        Dict[str, Any]: A dictionary containing the JSON response from the API call, which includes the user's information. If the request fails, the dictionary will contain an 'error' key with the error message.

    Raises:
        Exception: If the API request fails or returns an error, an exception is raised with the error details.
    '''
    logger.debug("Making GET request to /api/v1/session/userinfo")

    params = {}
    data = {}

    flat_body = {}
    data = assemble_nested_body(flat_body)

    success, response = await make_api_request("/api/v1/session/userinfo", method="GET", params=params, data=data)

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response
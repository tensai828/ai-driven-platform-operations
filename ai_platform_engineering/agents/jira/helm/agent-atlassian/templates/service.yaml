apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-jira.fullname" . }}
  labels:
    {{- include "agent-jira.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-jira.selectorLabels" . | nindent 4 }}

{{- if and (eq (include "graphrag.llmSecrets.externalSecrets.enabled" .) "false") (eq (include "graphrag.llmSecrets.create" .) "true") }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "graphrag.llmSecrets.secretName" . }}
  labels:
    {{- include "graphrag.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.llmSecrets.data }}
  {{- if $value }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
  {{- end }}
{{- end }}

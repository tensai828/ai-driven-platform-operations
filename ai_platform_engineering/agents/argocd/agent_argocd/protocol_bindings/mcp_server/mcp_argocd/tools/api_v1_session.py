
# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/session operations"""

import logging
from typing import Dict, Any
from agent_argocd.protocol_bindings.mcp_server.mcp_argocd.api.client import make_api_request

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def sessionservice_create(body: str) -> Dict[str, Any]:
    '''
    Create a new JWT for authentication and set a cookie if using HTTP.

    Args:
        body (str): The request payload containing authentication details.

    Returns:
        Dict[str, Any]: The response from the session creation endpoint, including the JWT and any relevant session data.

    Raises:
        Exception: If the API request fails or an unexpected error occurs during session creation.
    '''
    logger.debug("Making POST request to /api/v1/session")
    params = {}
    
    if body is not None:
      params["body"] = body
    
    data = None

    # Add parameters to request
    if body is not None:
        data = body

    success, response = await make_api_request(
        "/api/v1/session",
        method="POST",
        params=params,
        data=data
    )
    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get('error', 'Request failed')}
    return response


async def sessionservice_delete() -> Dict[str, Any]:
    '''
    Delete an existing JWT cookie if using HTTP.

    Args:
        None

    Returns:
        Dict[str, Any]: The response from the API after attempting to delete the session. Contains error information if the request fails.

    Raises:
        Exception: If there is an unexpected error during the API request.
    '''
    logger.debug("Making DELETE request to /api/v1/session")
    params = {}
    
    data = None

    success, response = await make_api_request(
        "/api/v1/session",
        method="DELETE",
        params=params,
        data=data
    )
    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get('error', 'Request failed')}
    return response

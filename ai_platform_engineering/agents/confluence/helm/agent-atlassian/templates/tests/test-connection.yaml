apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agent-confluence.fullname" . }}-test-connection"
  labels:
    {{- include "agent-confluence.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agent-confluence.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

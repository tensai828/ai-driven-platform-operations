# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /locations/{id} operations"""

import logging
from typing import Dict, Any
from agent_backstage.protocol_bindings.mcp_server.mcp_backstage.api.client import make_api_request

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def get_location(path_id: str) -> Dict[str, Any]:
    '''
    Retrieve a location by its unique identifier.

    Args:
        path_id (str): The unique identifier of the location to retrieve.

    Returns:
        Dict[str, Any]: The JSON response containing the location details.

    Raises:
        Exception: If the API request fails or returns an error.

    OpenAPI Specification:
      get:
        summary: Get a location by id.
        operationId: getLocation
        parameters:
          - name: path_id
            in: path
            required: true
            description: The unique identifier of the location.
            schema:
              type: string
        responses:
          '200':
            description: Successful response with location details.
            content:
              application/json:
                schema:
                  type: object
          '404':
            description: Location not found.
            content:
              application/json:
                schema:
                  type: object
          '500':
            description: Server error.
            content:
              application/json:
                schema:
                  type: object
    '''
    logger.debug("Making GET request to /locations/{id}")

    params = {}
    data = {}

    success, response = await make_api_request(f"/api/catalog/locations/{path_id}", method="GET", params=params, data=data)

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response


async def delete_location(path_id: str) -> Dict[str, Any]:
    '''
    Deletes a location by its unique identifier.

    Args:
        path_id (str): The unique identifier of the location to delete.

    Returns:
        Dict[str, Any]: The JSON response from the API call, typically indicating success or failure.

    Raises:
        Exception: If the API request fails or returns an error.

    OpenAPI Specification:
      delete:
        summary: Delete a location by id.
        description: Deletes a location resource identified by the provided path_id.
        operationId: deleteLocation
        parameters:
          - name: path_id
            in: path
            required: true
            description: The unique identifier of the location to delete.
            schema:
              type: string
        responses:
          '200':
            description: Location deleted successfully.
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    message:
                      type: string
                      example: Location deleted successfully.
          '404':
            description: Location not found.
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    error:
                      type: string
                      example: Location not found.
          '400':
            description: Invalid request.
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    error:
                      type: string
                      example: Invalid location id.
          '500':
            description: Internal server error.
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    error:
                      type: string
                      example: Internal server error.
    '''
    logger.debug("Making DELETE request to /locations/{id}")

    params = {}
    data = {}

    success, response = await make_api_request(f"/api/catalog/locations/{path_id}", method="DELETE", params=params, data=data)

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kb-rag-redis.fullname" . }}
  labels:
    {{- include "kb-rag-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "kb-rag-redis.selectorLabels" . | nindent 4 }}

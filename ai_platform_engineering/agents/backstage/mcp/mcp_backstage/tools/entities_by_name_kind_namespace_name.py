# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /entities/by-name/{kind}/{namespace}/{name} operations"""

import logging
from typing import Any
from mcp_backstage.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def get_entity_by_name(path_kind: str, path_namespace: str, path_name: str) -> Any:
  '''
  Retrieve an entity by its kind, namespace, and name reference.

  Args:
      path_kind (str): The kind of the entity to retrieve (e.g., 'Component', 'Resource').
      path_namespace (str): The namespace of the entity.
      path_name (str): The name of the entity.

  Returns:
      Any: The JSON response containing the entity details if found, or an error message.

  Raises:
      Exception: If the API request fails or returns an error response.
  '''
  logger.debug("Making GET request to /entities/by-name/{kind}/{namespace}/{name}")

  params = {}
  data = {}

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(
    f"/entities/by-name/{path_kind}/{path_namespace}/{path_name}", method="GET", params=params, data=data
  )

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
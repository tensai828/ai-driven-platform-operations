# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /entity-facets operations"""

import logging
from typing import Any, List
from mcp_backstage.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def get_entity_facets(param_facet: List[str], param_filter: List[str] = None) -> Any:
  '''
  Retrieve all entity facets that match the specified filters.

  Args:
      param_facet (List[str]): List of facet names to retrieve for the entities.
      param_filter (List[str], optional): One or more filter sets to match against each entity.
          Each filter set is a list of conditions that must all be true for the set to match (AND logic within a set).
          At least one filter set must match for the entity to be included in the result (OR logic between sets).
          Each condition can be in the form `<key>` (checks for existence) or `<key>=<value>` (checks for a specific value).
          Keys use a simplified JSON path and are case-insensitive. Defaults to None.

  Returns:
      Any: The JSON response from the API call containing the matching entity facets.

  Raises:
      Exception: If the API request fails or returns an error.
  '''
  logger.debug("Making GET request to /entity-facets")

  params = {}
  data = {}

  if param_facet is not None:
    params["facet"] = str(param_facet).lower() if isinstance(param_facet, bool) else param_facet

  if param_filter is not None:
    params["filter"] = str(param_filter).lower() if isinstance(param_filter, bool) else param_filter

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/entity-facets", method="GET", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
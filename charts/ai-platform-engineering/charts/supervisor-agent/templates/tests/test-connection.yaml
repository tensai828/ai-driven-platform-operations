apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "supervisorAgent.fullname" . }}-test-connection"
  labels:
    {{- include "supervisorAgent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "supervisorAgent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

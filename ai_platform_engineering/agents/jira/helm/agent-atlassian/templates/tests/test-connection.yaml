apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agent-jira.fullname" . }}-test-connection"
  labels:
    {{- include "agent-jira.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agent-jira.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "caipe-ui.fullname" . }}
  labels:
    {{- include "caipe-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caipe-ui.selectorLabels" . | nindent 4 }}

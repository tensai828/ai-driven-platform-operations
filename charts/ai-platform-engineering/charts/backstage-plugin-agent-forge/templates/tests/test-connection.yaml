apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backstage-plugin-agent-forge.fullname" . }}-test-connection"
  labels:
    {{- include "backstage-plugin-agent-forge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backstage-plugin-agent-forge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never


# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/applicationsets/generate operations"""

import logging
from typing import Dict, Any
from agent_argocd.protocol_bindings.mcp_server.mcp_argocd.api.client import make_api_request

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def applicationsetservice_generate(body: str) -> Dict[str, Any]:
    '''
    Generates an application set based on the provided configuration.

    Args:
        body (str): The request payload containing the application set configuration.

    Returns:
        Dict[str, Any]: The response from the application set generation API, containing the generated application set or an error message.

    Raises:
        Exception: If the API request fails or an unexpected error occurs during the generation process.
    '''
    logger.debug("Making POST request to /api/v1/applicationsets/generate")
    params = {}
    
    if body is not None:
      params["body"] = body
    
    data = None

    # Add parameters to request
    if body is not None:
        data = body

    success, response = await make_api_request(
        "/api/v1/applicationsets/generate",
        method="POST",
        params=params,
        data=data
    )
    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get('error', 'Request failed')}
    return response

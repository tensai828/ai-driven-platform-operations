apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-platform-engineer.fullname" . }}
  labels:
    {{- include "ai-platform-engineer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-platform-engineer.selectorLabels" . | nindent 4 }}

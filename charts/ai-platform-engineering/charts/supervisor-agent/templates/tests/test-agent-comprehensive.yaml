apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "supervisorAgent.fullname" . }}-test-comprehensive"
  labels:
    {{- include "supervisorAgent.labels" . | nindent 4 }}
    app.kubernetes.io/component: test
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "2"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
  - name: test-supervisor-agent-comprehensive
    image: curlimages/curl:latest
    command: ['sh', '-c']
    args:
    - |
      set -e
      echo "Testing supervisor-agent comprehensive functionality..."

      # Test main supervisor-agent service
      echo "Testing main supervisor-agent service..."
      curl -f http://{{ include "supervisorAgent.fullname" . }}:{{ .Values.service.port }}/health || echo "Health endpoint not available, trying basic connectivity"
      curl -f http://{{ include "supervisorAgent.fullname" . }}:{{ .Values.service.port }} || echo "Basic connectivity test"

      # Test environment variables
      echo "Checking environment configuration..."
      {{- if .Values.env }}
      {{- range $key, $value := .Values.env }}
      echo "Environment variable {{ $key }}: {{ $value }}"
      {{- end }}
      {{- end }}

      echo "✓ supervisor-agent comprehensive tests completed"

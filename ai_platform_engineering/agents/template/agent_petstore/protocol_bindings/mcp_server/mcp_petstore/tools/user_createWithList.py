# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /user/createWithList operations"""

import logging
from typing import Dict, Any, List
from agent_petstore.protocol_bindings.mcp_server.mcp_petstore.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def create_users_with_list_input(body: List[str]) -> Dict[str, Any]:
    '''
    Creates a list of users with the given input array.

    Args:
        body (List[str]): A list of user data to be created.

    Returns:
        Dict[str, Any]: The JSON response from the API call containing the result of the user creation operation.

    Raises:
        Exception: If the API request fails or returns an error.

    OpenAPI Specification:
      post:
        summary: Creates list of users with given input array.
        operationId: createUsersWithListInput
        requestBody:
          required: true
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
        responses:
          '200':
            description: Successful operation
            content:
              application/json:
                schema:
                  type: object
          '400':
            description: Invalid input
            content:
              application/json:
                schema:
                  type: object
    '''
    logger.debug("Making POST request to /user/createWithList")

    params = {}
    data = {}

    flat_body = {}
    data = assemble_nested_body(flat_body)

    success, response = await make_api_request("/user/createWithList", method="POST", params=params, data=data)

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response
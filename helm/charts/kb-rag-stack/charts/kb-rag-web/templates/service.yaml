apiVersion: v1
kind: Service
metadata:
  name: {{ include "kb-rag-web.fullname" . }}
  labels:
    {{- include "kb-rag-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kb-rag-web.selectorLabels" . | nindent 4 }}



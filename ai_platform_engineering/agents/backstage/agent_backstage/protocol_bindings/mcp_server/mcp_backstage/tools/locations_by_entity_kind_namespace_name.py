# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /locations/by-entity/{kind}/{namespace}/{name} operations"""

import logging
from typing import Dict, Any
from agent_backstage.protocol_bindings.mcp_server.mcp_backstage.api.client import make_api_request

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def get_location_by_entity(path_kind: str, path_namespace: str, path_name: str) -> Dict[str, Any]:
    '''
    Retrieves the location information for a specified entity.

    Args:
        path_kind (str): The kind of the entity (e.g., 'Component', 'Resource').
        path_namespace (str): The namespace of the entity.
        path_name (str): The name of the entity.

    Returns:
        Dict[str, Any]: The JSON response containing the location details for the specified entity.

    Raises:
        Exception: If the API request fails or returns an error.

    OpenAPI Specification:
      get:
        summary: Get a location for entity.
        operationId: getLocationByEntity
        parameters:
          - name: kind
            in: path
            required: true
            schema:
              type: string
            description: The kind of the entity (e.g., 'Component', 'Resource').
          - name: namespace
            in: path
            required: true
            schema:
              type: string
            description: The namespace of the entity.
          - name: name
            in: path
            required: true
            schema:
              type: string
            description: The name of the entity.
        responses:
          '200':
            description: Successful response with location details.
            content:
              application/json:
                schema:
                  type: object
          '404':
            description: Entity not found.
            content:
              application/json:
                schema:
                  type: object
          '500':
            description: Internal server error.
            content:
              application/json:
                schema:
                  type: object
        tags:
          - Locations
    '''
    logger.debug("Making GET request to /locations/by-entity/{kind}/{namespace}/{name}")

    params = {}
    data = {}

    success, response = await make_api_request(
        f"/api/catalog/locations/by-entity/{path_kind}/{path_namespace}/{path_name}", method="GET", params=params, data=data
    )

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response
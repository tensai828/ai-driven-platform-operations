apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-webui.fullname" . }}
  labels:
    {{- include "rag-webui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-webui.selectorLabels" . | nindent 4 }}



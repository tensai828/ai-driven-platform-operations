{{- if and (eq (include "agent.agentSecrets.externalSecrets.enabled" .) "false") (eq (include "agent.agentSecrets.create" .) "true") }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent.agentSecrets.secretName" . }}
  labels:
    {{- include "agent.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.agentSecrets.data }}
  {{- if $value }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
  {{- end }}
{{- end }}

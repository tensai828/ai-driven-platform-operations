tests:
  jira_agent_1:
    input: |
      show jira version
    reference_trajectory:
      solution_1: __start__;agent_jira
    metadata:
      comments: |
        Shows the version of the Jira Server Version.

  jira_agent_2:
    input: |
      show jira app health status in project jarvis-agent-dev
    reference_trajectory:
      solution_1: __start__;agent_jira
    metadata:
      comments: |
        Shows the health status of all applications in the jarvis-agent-dev project.

  jira_agent_3:
    input: |
      show jira unhealthy apps in project jarvis-agent-dev
    reference_trajectory:
      solution_1: __start__;agent_jira
    metadata:
      comments: |
        Lists all unhealthy applications in the jarvis-agent-dev project.

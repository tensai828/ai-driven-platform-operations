
# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/applications/manifestswithfiles operations"""

import logging
from typing import Dict, Any
from agent_argocd.protocol_bindings.mcp_server.mcp_argocd.api.client import make_api_request

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def applicationservice_getmanifestswithfiles(body: str) -> Dict[str, Any]:
    '''
    Retrieves application manifests generated from the provided files.

    Args:
        body (str): The file content or configuration data used to generate application manifests.

    Returns:
        Dict[str, Any]: A dictionary containing the generated application manifests or an error message.

    Raises:
        Exception: If the API request fails or an unexpected error occurs during manifest generation.
    '''
    logger.debug("Making POST request to /api/v1/applications/manifestswithfiles")
    params = {}
    
    if body is not None:
      params["body"] = body
    
    data = None

    # Add parameters to request
    if body is not None:
        data = body

    success, response = await make_api_request(
        "/api/v1/applications/manifestswithfiles",
        method="POST",
        params=params,
        data=data
    )
    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get('error', 'Request failed')}
    return response
